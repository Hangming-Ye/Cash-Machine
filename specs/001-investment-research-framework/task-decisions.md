# Tasks 阶段新增决策

2026-09-20。用户已明确“审核通过，转 task”。Plan 与 P-01—P-09 记录为已批准；以下是本轮任务组织的细化，未开始执行任务。没有新增产品需求、数据供应商、部署平台、模型服务或研究范围限制。

| ID | 本轮任务拆分决定 | 原方案关系／影响 | 对应任务 |
| --- | --- | --- | --- |
| TD-01 | 六个 P1 故事按 US4→US5→US2→US1→US3→US6 排列；首个阶段交付是带真实持仓、补证、估值和最小记忆的研究闭环 | 细化 P-09；US6 完整效果靠后，但最低读写前置，不推迟所有记忆工作。首个闭环不是最终完成 | T010—T011、T012—T056 |
| TD-02 | 现场访问与本地开发分开作为依赖；清点可记录缺口，缺口只阻塞受影响验收 | 细化既有数据约束；不因云端访问暂不可用阻塞全部代码，也不把失败来源登记完就当验收通过 | T001—T003、T025、T034、T049、T057—T061 |
| TD-03 | 多人并行只分配不同文件；CLI、共享模型、产物与记忆模块由当阶段负责人顺序集成 | 工程协作规则，不增加运行时角色／锁服务／状态机 | 各阶段并行表、T024、T033、T048、T054 |
| TD-04 | 合成回归材料入仓库；真实账户、Bot 映射和产品运行证据留私有目录，最终只提交脱敏验收映射 | 将既有隐私与验证边界落实成具体文件路径；验收仍需能访问对应真实证据 | T004、T016、T025、T034、T039、T049、T055—T061 |
| TD-05 | 原生方法先用禁用计划的测试 Routine 做 Test run；最终再验证一次测试目录内的真实单次定时运行，结束停用 | 区分“手动测试调用”和 SC-006 的“定时触发”，不增加长期测试任务，不改无关生产 Routine | T016、T058 |
| TD-06 | 本机可达的 `data/preflight/` 与 `data/validation/` 替代原先不可达的云端 `private:`／`test-private:` 前缀；云端 root 仅在该文件系统真实存在时使用 | 细化 TD-02／TD-04 的证据路径；live／原生行为在未实际运行前仍不算已验证 | T001—T002、T016、T025、T034、T038—T039、T049、T055—T061 |
| TD-07 | 专员行为验证由幕僚长写独立简报并委派新子 agent；子 agent 不读父会话 | 用户 2026-09-22 明确指令，优先于“必须等原生 Grok Bot”的任务文字。证据等级是 subagent specialist，不能冒充原生 Grok、定时 Routine、手机端或 live 数据 | T012、T016、T039、T049、T055 |
| TD-08 | 本次开发只在本机 Cursor / dev 验证；本地通过即勾选，live / 云端 / 原生 Grok / 手机 / 定时 Routine 不是完成条件 | 用户 2026-09-22 明确指令，优先于任务原文。证据等级仍写 local Cursor，不得改称 live 或原生通过。不新增供应商，不放宽订单与秘密边界 | T012、T016、T025、T034、T039、T049、T055—T061 |
| TD-09 | 供应链与单票须按多阶段深度完成：BFS 工艺地图（公司／角色／同一步竞争，朝材料推进）→ 下游需求 → 一次瓶颈检验 → 仅检验通过的短缺节点入队再拆 → 是否已定价 → 程序估值；单票须有经营驱动桥与 `compute valuation` 对照报价。阶段与短缺队列交接冻结文件，不建调度器／图库／状态机。覆盖旧“有限已证地图／停在不透明组件／检验前入队／早期线索可跳过估值／`not_applicable` 即可结束被请求分析”的出口。T039 本地证据与 2026-09-22 现场筛查不满足新深度；新增未勾选 T062—T064 | 用户 2026-09-22 最新指令（含同日 BFS 至材料补充及检验后入队），优先于 plan／R-04 中可被读成浅完成或顺序颠倒的旧措辞。A-07、修订后的 US1—US2／FR-003—004／FR-006／FR-014 | T062、T063、T064 |

文件进一步拆分如 sources 各现有适配器、calculations 的表达／运算／对齐／试验模块，均是已批准目录与职责的实施落点，不是新增库或微服务。计算运算集、退出码、数据缺口、记忆文件与版本规则沿用已批准契约；任务中未加入固定股票数、强制模型版本、统一收益目标或额外 SLA。

验收数量沿用 spec：9 个完整核心案例、8 类异常、三类独立专员与 3 条复盘。资源默认值仍通过样例测量后写入配置，本轮没有新加数值阈值。

## 2026-09-21 已批准的一致性修订

用户批准修复 analyze 报告的 I1／U1／U2／I2／C1／I3。改为迁移实际 prompts 文件；复用 check artifact 的显式 --archive 归档；区分三种记忆读取模式和版本 known_at；验收汇总可在阻塞时更新；六份请求模板和测试 runner 分配到现有任务；实验记录保存完整请求及哈希。保持 61 个任务、原架构和数据源，不新增服务或产品能力，不执行实施任务。

## 2026-09-22 本机可达证据路径

用户要求：本机无法打开的路径不得作为任务未完成的理由。`private:` / `test-private:` 原先指向云端 `/workspace/cash-machine/` 与 `/workspace/cash-machine-test/`。本机开发与离线验收改为仓库内 gitignore 的 `data/preflight/` 与 `data/validation/`。云端 root 只在那台机器真实存在时使用。账户、凭证和 Bot 映射仍不入 Git。改路径不代替原生 Grok、live 来源或真实持仓验收。

## 2026-09-22 子 agent 验证

用户要求验证专员行为时，主 agent 担任幕僚长，按 research-entry 写出完整独立简报，再新建子 agent 执行。子 agent 不依赖父会话，也不依赖 Grok Bot、Routine 或云端部署。该证据只证明子 agent 按岗位方法执行，不证明原生产品行为。live 数据、真实持仓和订单边界仍按原任务，不因换了执行者就算通过。

## 2026-09-22 本机开发验收

用户要求：本次开发只在 dev 本地环境验证，Cursor 本机验证通过即打勾，无需 live 环境。原任务把云端部署、原生 Grok、真实持仓、手机／PC 和定时 Routine 写成完成条件，属于任务文字错误，按本决定改写。改写后的任务仍必须有本机实际证据才能勾选。
