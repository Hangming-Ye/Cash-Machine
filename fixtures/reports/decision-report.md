# Synthetic portfolio decision

This body is fixture content only. It must be token-rendered and copied into the isolated run root by T060 before `report-check.json` is executed. It is not investment research evidence.

The synthetic result is evidence-limited and leaves unsupported price fields empty.
